# Раскатка графа метро/МЦК/МЦД на живых данных — замеры

Дата прогона: 2026-08-31 (номинальная дата задачи 2026-08-29, план исполнялся позже).

## Условия прогона

`ORS_API_KEY` присутствует в `.env`, но `habitus metro` запускался с флагом
`--no-ors` (см. controller ruling R86): прямой прогон без флага сконструировал
бы реальный `ORSWalker` и сделал бы ~200k вызовов к Overpass/ORS на 6738
объявлений (по вызову на пару объект-станция) — далеко за пределами любой
публичной квоты. Поэтому **все пешие плечи в этом прогоне — оценка по прямой
(`estimated = true`)**, а не измерение ORS. Доля `estimated` ниже по заметке —
следствие способа запуска, а не свойство данных.

## Шаг 1: Сборка графа

Команды:
```
uv run habitus metro --city msk --no-ors
uv run habitus metro --city spb --no-ors
```

**msk** (`failed: []`):
```
{'subway': {'lines': 32, 'stations': 554, 'edges': 1052, 'transfers': 90},
 'mck':    {'lines': 2,  'stations': 62,  'edges': 124,  'transfers': 118},
 'mcd':    {'lines': 30, 'stations': 625, 'edges': 1190, 'transfers': 206},
 'access': 20365, 'walk_min_metro': 6738}
```
Статистика непустая по всем трём системам, `failed` пуст — условие шага
выполнено.

**spb**: (заполняется по мере прогона)

## Шаг 2: Связность и доля оценок

(заполняется по мере прогона)

## R37 — кольцо на живых данных

(заполняется по мере прогона)

## R27 — пересадки МЦД↔метро

(заполняется по мере прогона)

## Шаг 3: Метрики поиска

(заполняется по мере прогона)

## Шаг 5: Тесты

(заполняется по мере прогона)

## Вывод

(заполняется по мере прогона)
