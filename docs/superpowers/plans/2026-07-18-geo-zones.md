# Гео-зоны запроса — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Превратить указание района в запросе («на севере», «в центре», «в Хамовниках», «у Патриков», «рядом со Сколково») в точный гео-фильтр по реальным полигонам округов/районов Москвы, разговорному словарю и геокод-fallback, со строгим фильтром и честным авто-расширением.

**Architecture:** Разовый импорт полигонов округов/районов (OSM) в `admin_zones` + курируемый словарь разговорных зон `named_zones`; предвычисление колонок `listings.okrug/raion` через `ST_Contains` при загрузке. Резолвер `resolve_area` — слоёная цепочка (сторона света→округа кодом; разговорная зона; имя района/округа→колонка; кольцо; Nominatim-fallback), возвращает SQL-предикат + цепочку авто-расширения. Оркестратор расширяет область (район→округ→снять) при нехватке результатов.

**Tech Stack:** Python 3.12 / uv, PostgreSQL + PostGIS + pgvector (dev-БД порт 5544), psycopg3, requests (Overpass/Nominatim), pytest.

**Спека:** `docs/superpowers/specs/2026-07-18-geo-zones-design.md` — источник правды.

## Global Constraints

- Python 3.12, пакетный менеджер `uv` (`uv run pytest`, `uv run habitus …`).
- Dev-БД: `postgresql://habitus:habitus@localhost:5544/habitus` (`settings.db_dsn`).
- ⚠️ Тесты с БД делают TRUNCATE/DROP — гонять ТОЛЬКО на отдельной БД:
  `CREATE DATABASE habitus_test;` + `DB_DSN="…/habitus_test" uv run pytest`. Не гонять полный pytest на наполненной dev-БД.
- Коммиты: сообщения на русском, БЕЗ упоминания Claude/AI, БЕЗ трейлеров Co-Authored-By.
- Стиль: короткие модули, комментарии на русском, конфиг через `habitus.config.settings`, гео-предикаты вливаются через `build_where(extra_sql=…, extra_params=…)`.
- Центр = ЦАО (широкий) по умолчанию; «внутри Садового» — отдельная зона.
- Кардинал-логика (утверждена): `север`→{САО,СВАО,СЗАО}, `юг`→{ЮАО,ЮВАО,ЮЗАО}, `запад`→{ЗАО,СЗАО,ЮЗАО}, `восток`→{ВАО,СВАО,ЮВАО}; диагонали→точный округ (`юго-запад`→{ЮЗАО}); `центр`→{ЦАО}.
- Существующий `resolve_area` (bbox по lat/lon, коммит c45530f) ЗАМЕНЯЕТСЯ этой реализацией; старые bbox-тесты в `tests/test_area.py` переписываются.
- `import-zones` — ПРЕДУСЛОВИЕ работы фичи: без него `listings.okrug/raion` пусты и область деградирует (авто-расширение снимет фильтр).

---

### Task 1: Схема — admin_zones, named_zones, колонки listings

**Files:**
- Modify: `habitus/db/schema.sql`
- Test: `tests/test_zones_schema.py`

**Interfaces:**
- Produces: таблицы `admin_zones(id, kind, name, parent, aliases, geom, updated_at)`, `named_zones(id, name, aliases, lon, lat, radius_m)`; колонки `listings.okrug`, `listings.raion`. На них опираются Tasks 2–7.

- [ ] **Step 1: Написать падающий тест схемы**

`tests/test_zones_schema.py`:
```python
import psycopg
from habitus.config import settings
from habitus.db.init_db import init_db


def _cols(conn, table):
    rows = conn.execute(
        "SELECT column_name FROM information_schema.columns WHERE table_name=%s",
        (table,)).fetchall()
    return {r[0] for r in rows}


def test_zone_tables_and_columns_exist():
    with psycopg.connect(settings.db_dsn) as conn:
        init_db(conn)
        assert {"kind", "name", "parent", "aliases", "geom"} <= _cols(conn, "admin_zones")
        assert {"name", "aliases", "lon", "lat", "radius_m"} <= _cols(conn, "named_zones")
        assert {"okrug", "raion"} <= _cols(conn, "listings")
```

- [ ] **Step 2: Запустить — FAIL**

Run: `DB_DSN="postgresql://habitus:habitus@localhost:5544/habitus_test" uv run pytest tests/test_zones_schema.py -v`
(Сначала: `PGPASSWORD=habitus psql -h localhost -p 5544 -U habitus -d habitus -c "CREATE DATABASE habitus_test;"`)
Expected: FAIL (таблиц/колонок нет).

- [ ] **Step 3: Добавить в `habitus/db/schema.sql`** (в конец, после существующих таблиц):

```sql
-- Полигоны админ-деления и колец Москвы (импорт из OSM, разово).
CREATE TABLE IF NOT EXISTS admin_zones (
    id         bigserial PRIMARY KEY,
    kind       text NOT NULL,                 -- 'okrug' | 'raion' | 'ring'
    name       text NOT NULL,                 -- 'ЦАО' | 'Хамовники' | 'Садовое кольцо'
    parent     text,                          -- для raion — имя округа; иначе NULL
    aliases    text[] NOT NULL DEFAULT '{}',
    geom       geometry(MultiPolygon, 4326) NOT NULL,
    updated_at timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX IF NOT EXISTS admin_zones_geom_gix ON admin_zones USING GIST (geom);
CREATE UNIQUE INDEX IF NOT EXISTS admin_zones_kind_name ON admin_zones (kind, lower(name));

-- Курируемый словарь разговорных/брендовых зон (Патрики, Золотая миля…).
CREATE TABLE IF NOT EXISTS named_zones (
    id       bigserial PRIMARY KEY,
    name     text NOT NULL,
    aliases  text[] NOT NULL DEFAULT '{}',
    lon      double precision NOT NULL,
    lat      double precision NOT NULL,
    radius_m double precision NOT NULL DEFAULT 700
);
CREATE UNIQUE INDEX IF NOT EXISTS named_zones_name ON named_zones (lower(name));

-- Предвычисленная принадлежность объявления округу/району.
ALTER TABLE listings ADD COLUMN IF NOT EXISTS okrug text;
ALTER TABLE listings ADD COLUMN IF NOT EXISTS raion text;
CREATE INDEX IF NOT EXISTS listings_okrug_ix ON listings (okrug);
CREATE INDEX IF NOT EXISTS listings_raion_ix ON listings (raion);
```

- [ ] **Step 4: Запустить — PASS**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_zones_schema.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add habitus/db/schema.sql tests/test_zones_schema.py
git commit -m "feat: схема гео-зон — admin_zones, named_zones, колонки okrug/raion"
```

---

### Task 2: Импорт зон из GeoJSON + словаря + backfill (`habitus/geo/zones.py`)

**Files:**
- Create: `habitus/geo/zones.py`
- Create: `tests/test_zones.py`
- Create: `tests/fixtures/zones_sample.geojson`
- Create: `data/named_zones.seed.json`

**Interfaces:**
- Consumes: таблицы Task 1; `psycopg`.
- Produces (на них опираются Tasks 4–7):
  - `import_admin_geojson(path: Path, conn) -> int` — грузит Feature'ы (`properties.kind`∈okrug|raion|ring, `properties.name`, геометрия) в `admin_zones`, идемпотентно по `(kind, lower(name))`.
  - `import_named_seed(path: Path, conn) -> int` — грузит `named_zones` из JSON-массива `{name, aliases, lon, lat, radius_m}`.
  - `backfill_listing_zones(conn) -> int` — проставляет `listings.okrug/raion` через `ST_Contains`; проставляет `admin_zones.parent` для raion (округ по центроиду). Возвращает число обновлённых объявлений.

- [ ] **Step 1: Фикстуры**

`tests/fixtures/zones_sample.geojson` — два округа-квадрата и один район внутри одного из них (координаты вокруг Москвы):
```json
{"type":"FeatureCollection","features":[
 {"type":"Feature","properties":{"kind":"okrug","name":"ЦАО"},
  "geometry":{"type":"Polygon","coordinates":[[[37.55,55.72],[37.68,55.72],[37.68,55.79],[37.55,55.79],[37.55,55.72]]]}},
 {"type":"Feature","properties":{"kind":"okrug","name":"САО"},
  "geometry":{"type":"Polygon","coordinates":[[[37.45,55.80],[37.70,55.80],[37.70,55.92],[37.45,55.92],[37.45,55.80]]]}},
 {"type":"Feature","properties":{"kind":"raion","name":"Хамовники"},
  "geometry":{"type":"Polygon","coordinates":[[[37.57,55.72],[37.62,55.72],[37.62,55.75],[37.57,55.75],[37.57,55.72]]]}}
]}
```

`data/named_zones.seed.json` (реальные якоря, ~радиусы; расширяемо):
```json
[
 {"name":"Патриаршие пруды","aliases":["Патрики","Патриаршие"],"lon":37.5935,"lat":55.7644,"radius_m":700},
 {"name":"Остоженка","aliases":["Золотая миля"],"lon":37.5985,"lat":55.7402,"radius_m":700},
 {"name":"Москва-Сити","aliases":["Сити","ММДЦ"],"lon":37.5372,"lat":55.7495,"radius_m":800},
 {"name":"Чистые пруды","aliases":["Чистопрудный"],"lon":37.6389,"lat":55.7649,"radius_m":600},
 {"name":"Замоскворечье","aliases":[],"lon":37.6280,"lat":55.7380,"radius_m":1200},
 {"name":"Арбат","aliases":["Старый Арбат"],"lon":37.5920,"lat":55.7490,"radius_m":700},
 {"name":"Тверская","aliases":[],"lon":37.6060,"lat":55.7640,"radius_m":800}
]
```

- [ ] **Step 2: Падающие тесты**

`tests/test_zones.py`:
```python
from pathlib import Path

import psycopg
import pytest

from habitus.config import settings
from habitus.db.init_db import init_db
from habitus.geo.zones import (backfill_listing_zones, import_admin_geojson,
                               import_named_seed)

FIX = Path(__file__).parent / "fixtures" / "zones_sample.geojson"
SEED = Path(__file__).resolve().parents[1] / "data" / "named_zones.seed.json"


@pytest.fixture
def conn():
    with psycopg.connect(settings.db_dsn) as c:
        init_db(c)
        with c.cursor() as cur:
            cur.execute("TRUNCATE admin_zones, named_zones, listings RESTART IDENTITY;")
        c.commit()
        yield c


def test_import_admin_and_backfill(conn):
    assert import_admin_geojson(FIX, conn) == 3
    with conn.cursor() as cur:
        cur.execute("""INSERT INTO listings (external_id, source, geom) VALUES
            ('IN_HAM','test', ST_SetSRID(ST_MakePoint(37.60,55.735),4326)),
            ('IN_SAO','test', ST_SetSRID(ST_MakePoint(37.60,55.86),4326));""")
    conn.commit()
    assert backfill_listing_zones(conn) == 2
    rows = dict(conn.execute("SELECT external_id, okrug FROM listings ORDER BY 1").fetchall())
    assert rows["IN_HAM"] == "ЦАО" and rows["IN_SAO"] == "САО"
    ham_raion = conn.execute("SELECT raion FROM listings WHERE external_id='IN_HAM'").fetchone()[0]
    assert ham_raion == "Хамовники"
    # parent района проставлен округом
    parent = conn.execute("SELECT parent FROM admin_zones WHERE kind='raion' AND name='Хамовники'").fetchone()[0]
    assert parent == "ЦАО"


def test_import_admin_is_idempotent(conn):
    import_admin_geojson(FIX, conn)
    import_admin_geojson(FIX, conn)
    n = conn.execute("SELECT count(*) FROM admin_zones").fetchone()[0]
    assert n == 3


def test_import_named_seed(conn):
    assert import_named_seed(SEED, conn) >= 5
    row = conn.execute("SELECT lon, lat FROM named_zones WHERE 'Патрики' = ANY(aliases)").fetchone()
    assert row is not None and 37.5 < row[0] < 37.7
```

- [ ] **Step 3: Запустить — FAIL**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_zones.py -v`
Expected: FAIL (`ModuleNotFoundError: habitus.geo.zones`).

- [ ] **Step 4: Реализовать `habitus/geo/zones.py`**

```python
# habitus/geo/zones.py — импорт полигонов админ-зон/словаря и backfill объявлений
import json
from pathlib import Path

import psycopg


def import_admin_geojson(path: Path, conn: psycopg.Connection) -> int:
    """FeatureCollection округов/районов/колец → admin_zones (идемпотентно)."""
    fc = json.loads(Path(path).read_text(encoding="utf-8"))
    rows = []
    for f in fc.get("features", []):
        p = f.get("properties") or {}
        kind, name = p.get("kind"), p.get("name")
        geom = f.get("geometry")
        if kind not in ("okrug", "raion", "ring") or not name or not geom:
            continue
        rows.append({"kind": kind, "name": name,
                     "aliases": p.get("aliases") or [],
                     "geometry": json.dumps(geom)})
    sql = """
        INSERT INTO admin_zones (kind, name, aliases, geom)
        VALUES (%(kind)s, %(name)s, %(aliases)s,
                ST_Multi(ST_SetSRID(ST_GeomFromGeoJSON(%(geometry)s), 4326)))
        ON CONFLICT (kind, lower(name)) DO UPDATE SET
            aliases=EXCLUDED.aliases, geom=EXCLUDED.geom, updated_at=now();
    """
    if rows:
        with conn.cursor() as cur:
            cur.executemany(sql, rows)
    conn.commit()
    return len(rows)


def import_named_seed(path: Path, conn: psycopg.Connection) -> int:
    """JSON-массив разговорных зон → named_zones (идемпотентно по name)."""
    items = json.loads(Path(path).read_text(encoding="utf-8"))
    sql = """
        INSERT INTO named_zones (name, aliases, lon, lat, radius_m)
        VALUES (%(name)s, %(aliases)s, %(lon)s, %(lat)s, %(radius_m)s)
        ON CONFLICT (lower(name)) DO UPDATE SET
            aliases=EXCLUDED.aliases, lon=EXCLUDED.lon, lat=EXCLUDED.lat,
            radius_m=EXCLUDED.radius_m;
    """
    rows = [{"name": it["name"], "aliases": it.get("aliases") or [],
             "lon": it["lon"], "lat": it["lat"],
             "radius_m": it.get("radius_m", 700)} for it in items]
    if rows:
        with conn.cursor() as cur:
            cur.executemany(sql, rows)
    conn.commit()
    return len(rows)


def backfill_listing_zones(conn: psycopg.Connection) -> int:
    """Проставить listings.okrug/raion через ST_Contains + parent района округом."""
    with conn.cursor() as cur:
        # parent района = округ, содержащий его центроид
        cur.execute("""
            UPDATE admin_zones r SET parent = o.name
            FROM admin_zones o
            WHERE r.kind='raion' AND o.kind='okrug'
              AND ST_Contains(o.geom, ST_PointOnSurface(r.geom));""")
        cur.execute("""
            UPDATE listings l SET
              okrug = (SELECT name FROM admin_zones z WHERE z.kind='okrug'
                       AND ST_Contains(z.geom, l.geom) LIMIT 1),
              raion = (SELECT name FROM admin_zones z WHERE z.kind='raion'
                       AND ST_Contains(z.geom, l.geom) LIMIT 1)
            WHERE l.geom IS NOT NULL;""")
        n = cur.rowcount
    conn.commit()
    return n
```

- [ ] **Step 5: Запустить — PASS**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_zones.py -v`
Expected: PASS (3 теста).

- [ ] **Step 6: CLI `import-zones`** — в `habitus/cli.py`:

В блок парсеров (рядом с `import-evidence`):
```python
    zones = sub.add_parser("import-zones")
    zones.add_argument("--geojson", type=Path, required=True)
    zones.add_argument("--named", type=Path,
                       default=Path("data/named_zones.seed.json"))
```
В диспетчер (рядом с веткой `import-evidence`):
```python
        elif args.cmd == "import-zones":
            from habitus.geo.zones import (backfill_listing_zones,
                                           import_admin_geojson,
                                           import_named_seed)
            init_db(conn)
            a = import_admin_geojson(args.geojson, conn)
            n = import_named_seed(args.named, conn) if args.named.exists() else 0
            b = backfill_listing_zones(conn)
            print({"admin_zones": a, "named_zones": n, "listings_backfilled": b})
```

- [ ] **Step 7: Commit**

```bash
git add habitus/geo/zones.py habitus/cli.py tests/test_zones.py tests/fixtures/zones_sample.geojson data/named_zones.seed.json
git commit -m "feat: импорт полигонов админ-зон и словаря разговорных зон + backfill округ/район"
```

---

### Task 3: Build-скрипт GeoJSON округов/районов из OSM

**Files:**
- Create: `scripts/build_zones_geojson.py`

**Interfaces:**
- Consumes: Overpass API (как `habitus/geo/osm_extract.py`), PostGIS для сборки полигонов из ways.
- Produces: `data/moscow_admin.geojson` — FeatureCollection округов (admin_level=5) и районов (admin_level=8) с `properties.kind/name`. Потребляется `import-zones` (Task 2).

Примечание: сборка мультиполигонов из Overpass-relations нетривиальна (кольца/дырки). Скрипт собирает полигон через PostGIS `ST_BuildArea(ST_LineMerge(ST_Collect(ways)))`; сложные зоны (пара штук) при провале логируются и пропускаются — их можно добить ручной выгрузкой с osm-boundaries.com. Юнит-тестами не покрываем (сетевой data-acquisition, как `scripts/build_evidence.py`); проверка — smoke-прогон при исполнении.

- [ ] **Step 1: Реализовать `scripts/build_zones_geojson.py`**

```python
#!/usr/bin/env python
"""Сборка data/moscow_admin.geojson: полигоны округов (admin_level=5) и районов
(admin_level=8) Москвы из OSM. Ways тянем Overpass'ом, полигоны собираем в PostGIS
(ST_BuildArea). Сложные relation'ы, где сборка не даёт валидный полигон, пропускаем
с логом — их добить ручной выгрузкой osm-boundaries.com при необходимости.

Запуск:  uv run python scripts/build_zones_geojson.py --out data/moscow_admin.geojson
Потом:   uv run habitus import-zones --geojson data/moscow_admin.geojson
"""
import argparse
import json
import time
from pathlib import Path

import psycopg
import requests

from habitus.config import settings
from habitus.geo.osm_extract import HEADERS, OVERPASS_URL, RETRY_STATUS

# Москва как area: OSM relation 102269 → area id 3600102269.
AREA = "area(3600102269)"


def _overpass(query: str, retries: int = 4, backoff: float = 5.0) -> dict:
    q = f"[out:json][timeout:180];{query}"
    last = ""
    for attempt in range(retries):
        try:
            r = requests.post(OVERPASS_URL, data={"data": q}, headers=HEADERS, timeout=240)
            if r.status_code in RETRY_STATUS:
                last = f"HTTP {r.status_code}"
            else:
                r.raise_for_status()
                return r.json()
        except (requests.exceptions.RequestException, ValueError) as e:
            last = f"{type(e).__name__}: {e}"
        time.sleep(backoff * (attempt + 1))
    raise RuntimeError(f"Overpass не ответил: {last}")


def _fetch(level: int) -> list[dict]:
    """Список relation'ов заданного admin_level с их ways (out geom)."""
    payload = _overpass(
        f'relation["boundary"="administrative"]["admin_level"="{level}"]{AREA};'
        f'out tags; way(r);out geom;')
    rels, ways = {}, {}
    cur_rel = None
    for el in payload.get("elements", []):
        if el["type"] == "relation":
            rels[el["id"]] = {"name": (el.get("tags") or {}).get("name"), "members": el.get("members", [])}
    # второй проход: ways по id
    for el in payload.get("elements", []):
        if el["type"] == "way" and el.get("geometry"):
            ways[el["id"]] = [[p["lon"], p["lat"]] for p in el["geometry"]]
    out = []
    for rid, rel in rels.items():
        outer = [ways[m["ref"]] for m in rel["members"]
                 if m.get("type") == "way" and m.get("role") in ("outer", "") and m["ref"] in ways]
        if rel["name"] and outer:
            out.append({"name": rel["name"], "ways": outer})
    return out


def _assemble(conn, ways: list[list]) -> str | None:
    """Собрать полигон из набора линий через PostGIS; None если невалидно."""
    lines = [json.dumps({"type": "LineString", "coordinates": w}) for w in ways]
    row = conn.execute(
        "SELECT ST_AsGeoJSON(ST_Multi(ST_BuildArea(ST_LineMerge(ST_Collect("
        "ARRAY(SELECT ST_SetSRID(ST_GeomFromGeoJSON(x),4326) FROM unnest(%s::text[]) x))))))",
        (lines,)).fetchone()
    return row[0] if row and row[0] else None


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--out", type=Path, default=Path("data/moscow_admin.geojson"))
    args = ap.parse_args()
    feats = []
    with psycopg.connect(settings.db_dsn) as conn:
        for level, kind in ((5, "okrug"), (8, "raion")):
            rels = _fetch(level)
            print(f"admin_level={level}: {len(rels)} relation'ов")
            for rel in rels:
                geom = _assemble(conn, rel["ways"])
                if not geom:
                    print(f"  ПРОПУСК (не собрался полигон): {rel['name']}")
                    continue
                feats.append({"type": "Feature",
                              "properties": {"kind": kind, "name": rel["name"]},
                              "geometry": json.loads(geom)})
    args.out.parent.mkdir(parents=True, exist_ok=True)
    args.out.write_text(json.dumps({"type": "FeatureCollection", "features": feats},
                                   ensure_ascii=False), encoding="utf-8")
    print(f"WROTE {args.out}: {len(feats)} зон")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Smoke-прогон (при исполнении, не в CI)**

Run: `uv run python scripts/build_zones_geojson.py --out data/moscow_admin.geojson`
Expected: печатает «admin_level=5: ~12», «admin_level=8: ~120», WRITE. Если много ПРОПУСК — задокументировать и добить ручной выгрузкой osm-boundaries.com (admin_level 5+8, GeoJSON, те же `properties.kind/name`).

- [ ] **Step 3: Commit**

```bash
git add scripts/build_zones_geojson.py
git commit -m "feat: build-скрипт GeoJSON округов/районов Москвы из OSM"
```

---

### Task 4: Резолвер — сторона света и центр (`resolve_area` часть 1)

**Files:**
- Modify: `habitus/online/geo.py`
- Modify: `tests/test_area.py` (переписать bbox-тесты под округа)

**Interfaces:**
- Produces (на них опираются Tasks 5–7):
  - `@dataclass AreaMatch: sql: str; params: tuple; label: str; widen: list[tuple[str, tuple, str]]`
  - `CARDINAL: dict[str, tuple[str, ...]]` — направление→кортеж имён округов.
  - `resolve_area(area: str, conn=None, *, geocoder=geocode_address) -> AreaMatch | None` — в этой задаче реализует ТОЛЬКО кардинал/центр (conn пока не используется; параметр введён для Tasks 5–6). Остальные ветки → `None` (добавятся дальше).

- [ ] **Step 1: Переписать кардинал-тесты в `tests/test_area.py`**

Заменить существующие `test_cardinal_*` и `test_area_predicate_filters_in_postgis` на:
```python
from habitus.online.geo import CARDINAL, AreaMatch, resolve_area


def test_cardinal_north_maps_to_three_okrugs():
    m = resolve_area("север")
    assert isinstance(m, AreaMatch)
    assert m.sql == "okrug = ANY(%s)"
    assert m.params == (["САО", "СВАО", "СЗАО"],)
    assert m.widen and m.widen[-1][0] == "TRUE"      # финальный шаг — снять область


def test_cardinal_diagonal_is_single_okrug():
    m = resolve_area("юго-запад москвы")
    assert m.params == (["ЮЗАО"],)


def test_center_maps_to_cao():
    m = resolve_area("в центре")
    assert m.params == (["ЦАО"],)
    assert "ЦАО" in m.label


def test_district_word_not_treated_as_cardinal_returns_none_without_conn():
    # «Северное Бутово» — не кардинал; без conn прочие ветки не отрабатывают → None
    assert resolve_area("Северное Бутово") is None


def test_empty_area_none():
    assert resolve_area("") is None
```
**Удалить все устаревшие тесты старого `resolve_area` (кортеж `(sql,params)` + bbox/geocoder):** `test_cardinal_*`, `test_named_place_geocoded_to_proximity`, `test_district_with_direction_word_is_geocoded_not_cardinal`, `test_geocode_failure_returns_none`, `test_orchestrator_applies_area_predicate`, `test_area_predicate_filters_in_postgis`, хелпер `_boom`. Их замены появятся: named-зона → Task 5, geocode-fallback → Task 6, авто-расширение → Task 6.

- [ ] **Step 2: Запустить — FAIL**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_area.py -v`
Expected: FAIL (нет `AreaMatch`/`CARDINAL`; новая сигнатура).

- [ ] **Step 3: Переписать голову `resolve_area` в `habitus/online/geo.py`**

Заменить старые `_cardinal_predicate`/`resolve_area` (bbox) на:
```python
from dataclasses import dataclass


@dataclass
class AreaMatch:
    sql: str
    params: tuple
    label: str
    widen: list  # list[tuple[str, tuple, str]] — шире→шире, финал ("TRUE", (), «вся Москва»)


CARDINAL: dict[str, tuple[str, ...]] = {
    "N": ("САО", "СВАО", "СЗАО"), "S": ("ЮАО", "ЮВАО", "ЮЗАО"),
    "W": ("ЗАО", "СЗАО", "ЮЗАО"), "E": ("ВАО", "СВАО", "ЮВАО"),
    "NE": ("СВАО",), "NW": ("СЗАО",), "SE": ("ЮВАО",), "SW": ("ЮЗАО",),
    "C": ("ЦАО",),
}
_DROP = ("TRUE", (), "по всей Москве")


def _okrug_match(okrugs: tuple[str, ...], label: str) -> AreaMatch:
    return AreaMatch("okrug = ANY(%s)", (list(okrugs),), label, [_DROP])


def _cardinal_dirs(area: str) -> set[str] | None:
    """Множество направлений из фразы или None, если есть не-направление."""
    words = re.findall(r"[а-яёa-z]+", area.lower())
    dirs: set[str] = set()
    for w in words:
        if w in _AREA_FILLER:
            continue
        d = _dir_of(w)
        if d is None:
            return None
        dirs.add(d)
    return dirs or None


def _cardinal_match(area: str) -> AreaMatch | None:
    dirs = _cardinal_dirs(area)
    if dirs is None:
        return None
    if dirs == {"C"}:
        return _okrug_match(CARDINAL["C"], "центр (ЦАО)")
    # диагональ: пара сторона+сторона → точный округ
    diag = {frozenset({"N", "E"}): "NE", frozenset({"N", "W"}): "NW",
            frozenset({"S", "E"}): "SE", frozenset({"S", "W"}): "SW"}
    key = frozenset(dirs)
    if key in diag:
        code = diag[key]
        return _okrug_match(CARDINAL[code], f"{code}: округ {CARDINAL[code][0]}")
    if len(dirs) == 1:
        d = next(iter(dirs))
        return _okrug_match(CARDINAL[d], f"сторона света ({', '.join(CARDINAL[d])})")
    return None


def resolve_area(area: str, conn=None, *, geocoder=geocode_address) -> AreaMatch | None:
    """Область запроса → AreaMatch (SQL-предикат + цепочка расширения) или None."""
    if not area or not area.strip():
        return None
    card = _cardinal_match(area)
    if card is not None:
        return card
    return None   # ветки named/admin/ring/fallback — Tasks 5–6
```
Сохранить существующие `_DIR_STEMS`, `_AREA_FILLER`, `_dir_of` (Task c45530f) — они переиспользуются. Обновить `_dir_of`/`_DIR_STEMS`, чтобы возвращать N/S/E/W/C (уже так). Удалить `MOSCOW_CENTER`-bbox-ветки, `_cardinal_predicate` (старый), `AREA_RADIUS_M`-использование в старом виде (радиус переедет в Task 5).

- [ ] **Step 4: Запустить — PASS**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_area.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add habitus/online/geo.py tests/test_area.py
git commit -m "feat: resolve_area — сторона света и центр через округа (AreaMatch)"
```

---

### Task 5: Резолвер — разговорные зоны, имена районов/округов, кольца (`resolve_area` часть 2)

**Files:**
- Modify: `habitus/online/geo.py`
- Modify: `tests/test_area.py`

**Interfaces:**
- Consumes: `AreaMatch`, `resolve_area` (Task 4); таблицы `named_zones`/`admin_zones`, колонки `okrug`/`raion` (Tasks 1–2).
- Produces: `resolve_area` с ветками 2–4 (разговорная зона, имя района/округа, кольцо), использующими `conn`.

- [ ] **Step 1: Тесты на scratch-БД с сидом зон** — в `tests/test_area.py`:

```python
import psycopg
from habitus.db.init_db import init_db
from habitus.geo.zones import import_admin_geojson, import_named_seed
from pathlib import Path

FIX = Path(__file__).parent / "fixtures" / "zones_sample.geojson"
SEED = Path(__file__).resolve().parents[1] / "data" / "named_zones.seed.json"


def _seeded_conn():
    c = psycopg.connect(settings.db_dsn)
    init_db(c)
    with c.cursor() as cur:
        cur.execute("TRUNCATE admin_zones, named_zones RESTART IDENTITY;")
    c.commit()
    import_admin_geojson(FIX, c)
    import_named_seed(SEED, c)
    return c


def test_named_zone_alias_to_dwithin():
    with _seeded_conn() as conn:
        m = resolve_area("у Патриков", conn)
        assert "ST_DWithin" in m.sql
        assert m.params[0] == 37.5935 and m.params[1] == 55.7644  # якорь Патриарших
        assert "Патриаршие" in m.label


def test_district_name_to_column():
    with _seeded_conn() as conn:
        m = resolve_area("в Хамовниках", conn)
        assert m.sql == "raion = %s" and m.params == ("Хамовники",)
        # расширение: район → его округ → снять
        assert m.widen[0][0] == "okrug = %s" and m.widen[0][1] == ("ЦАО",)
        assert m.widen[-1][0] == "TRUE"


def test_okrug_name_to_column():
    with _seeded_conn() as conn:
        m = resolve_area("ЦАО", conn)
        assert m.sql == "okrug = %s" and m.params == ("ЦАО",)


def test_inside_sadovoe_uses_ring_polygon():
    with _seeded_conn() as conn:
        conn.execute("""INSERT INTO admin_zones (kind,name,geom) VALUES
            ('ring','Садовое кольцо', ST_Multi(ST_SetSRID(ST_MakePolygon(
             ST_GeomFromText('LINESTRING(37.60 55.75,37.64 55.75,37.64 55.77,37.60 55.77,37.60 55.75)')),4326)))""")
        conn.commit()
        m = resolve_area("внутри садового", conn)
        assert "ST_Within" in m.sql and "Садовое" in m.label
```

- [ ] **Step 2: Запустить — FAIL**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_area.py -v`
Expected: FAIL (ветки возвращают None).

- [ ] **Step 3: Дописать ветки в `resolve_area`** (`habitus/online/geo.py`), заменив финальный `return None`:

```python
    if conn is None:
        return None
    t = area.strip().lower()

    # 2. разговорная зона (named_zones по name/aliases) → ST_DWithin
    row = conn.execute(
        "SELECT name, lon, lat, radius_m FROM named_zones "
        "WHERE lower(name)=%s OR %s = ANY(ARRAY(SELECT lower(a) FROM unnest(aliases) a)) LIMIT 1",
        (t, t)).fetchone()
    if row:
        name, lon, lat, radius = row
        return AreaMatch(
            "ST_DWithin(geom::geography, ST_SetSRID(ST_MakePoint(%s,%s),4326)::geography, %s)",
            (lon, lat, radius), name,
            [("ST_DWithin(geom::geography, ST_SetSRID(ST_MakePoint(%s,%s),4326)::geography, %s)",
              (lon, lat, radius * 2), f"{name} (шире)"), _DROP])

    # 4. кольцо: «внутри садового» / «за мкад»
    if "садов" in t:
        rz = conn.execute("SELECT name FROM admin_zones WHERE kind='ring' AND lower(name) LIKE '%садов%' LIMIT 1").fetchone()
        if rz:
            return AreaMatch(
                "ST_Within(geom, (SELECT geom FROM admin_zones WHERE kind='ring' AND name=%s))",
                (rz[0],), rz[0], [("okrug = %s", ("ЦАО",), "ЦАО"), _DROP])
    if "мкад" in t and ("за " in t or t.startswith("за")):
        return _okrug_match(("ЗелАО", "НАО", "ТАО"), "за МКАД")

    # 3. имя района/округа (по admin_zones name/aliases) → колонка
    zr = conn.execute(
        "SELECT kind, name, parent FROM admin_zones "
        "WHERE lower(name)=%s OR %s = ANY(ARRAY(SELECT lower(a) FROM unnest(aliases) a)) "
        "ORDER BY CASE kind WHEN 'raion' THEN 0 ELSE 1 END LIMIT 1",
        (t, t)).fetchone()
    if zr:
        kind, name, parent = zr
        if kind == "raion":
            widen = []
            if parent:
                widen.append(("okrug = %s", (parent,), f"округ {parent}"))
            widen.append(_DROP)
            return AreaMatch("raion = %s", (name,), name, widen)
        return AreaMatch("okrug = %s", (name,), name, [_DROP])

    return None
```
(Порядок веток в коде: сначала named (2), затем ring (4), затем admin-name (3) — чтобы «Садовое» не спуталось с именем; итоговая нумерация в спеке — по точности, но реализация ставит ring перед admin-name осознанно.)

- [ ] **Step 4: Запустить — PASS**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_area.py -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add habitus/online/geo.py tests/test_area.py
git commit -m "feat: resolve_area — разговорные зоны, имена районов/округов, кольца"
```

---

### Task 6: Резолвер — Nominatim-fallback + оркестратор авто-расширение

**Files:**
- Modify: `habitus/online/geo.py`
- Modify: `habitus/online/orchestrator.py`
- Modify: `tests/test_area.py`, `tests/test_orchestrator.py`

**Interfaces:**
- Consumes: `resolve_area` (Tasks 4–5); `retrieve_with_relaxation` (существующий).
- Produces: `resolve_area` с fallback-веткой; оркестратор применяет `AreaMatch.widen` при `< min_results`.

- [ ] **Step 1: Тест fallback + авто-расширения**

В `tests/test_area.py`:
```python
def test_geocode_polygon_fallback():
    poly = {"type": "Polygon", "coordinates": [[[37.3,55.6],[37.4,55.6],[37.4,55.7],[37.3,55.7],[37.3,55.6]]]}
    def geo(q, polygon=False): return {"geometry": poly} if polygon else (37.35, 55.65)
    with _seeded_conn() as conn:
        m = resolve_area("Сколково", conn, geocoder=geo)
        assert "ST_Within" in m.sql or "ST_DWithin" in m.sql
```

В `tests/test_orchestrator.py`:
```python
from habitus.online.geo import AreaMatch
from habitus.online.schema import ParsedQuery


def test_area_auto_widens_when_too_few():
    seen = []
    def fake_search(conn, pq, *, geo_sql=None, geo_params=(), **kw):
        seen.append(geo_sql)
        return [] if geo_sql and "raion" in geo_sql else [_cand("A"), _cand("B")]
    am = AreaMatch("raion = %s", ("Хамовники",), "Хамовники",
                   [("okrug = %s", ("ЦАО",), "округ ЦАО"), ("TRUE", (), "вся Москва")])
    cands, relaxed, _ = retrieve_with_relaxation(
        None, ParsedQuery(area="Хамовники"), area_match=am,
        search_fn=fake_search, min_results=1, max_iters=0)
    assert len(cands) == 2                       # расширились до округа
    assert any("ЦАО" in r for r in relaxed)      # пометка расширения
```
(`_cand` уже есть в `tests/test_orchestrator.py`.)

- [ ] **Step 2: Запустить — FAIL**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_area.py tests/test_orchestrator.py -v`
Expected: FAIL.

- [ ] **Step 3: Fallback-ветка в `resolve_area`** — перед финальным `return None`:

```python
    # 5. Nominatim-fallback: полигон места, иначе точка+радиус
    try:
        poly = geocoder(f"{area}, Москва", polygon=True) if _accepts_polygon(geocoder) else None
    except TypeError:
        poly = None
    if isinstance(poly, dict) and poly.get("geometry"):
        return AreaMatch(
            "ST_Within(geom, ST_SetSRID(ST_GeomFromGeoJSON(%s),4326))",
            (json.dumps(poly["geometry"]),), area,
            [("ST_DWithin(geom::geography, ST_Centroid(ST_SetSRID(ST_GeomFromGeoJSON(%s),4326))::geography, %s)",
              (json.dumps(poly["geometry"]), 5000.0), f"{area} (окрестность)"), _DROP])
    coords = geocoder(f"{area}, Москва")
    if coords:
        lon, lat = coords
        return AreaMatch(
            "ST_DWithin(geom::geography, ST_SetSRID(ST_MakePoint(%s,%s),4326)::geography, %s)",
            (lon, lat, 3000.0), area,
            [("ST_DWithin(geom::geography, ST_SetSRID(ST_MakePoint(%s,%s),4326)::geography, %s)",
              (lon, lat, 6000.0), f"{area} (шире)"), _DROP])
    return None
```
Хелпер (рядом): `def _accepts_polygon(fn): import inspect; return "polygon" in inspect.signature(fn).parameters` — обёрнуть в try/except, при ошибке False.

- [ ] **Step 4: Оркестратор — принять `area_match` и расширять**

`habitus/online/orchestrator.py`: заменить резолв `pq.area` на приём готового `area_match` и добавить расширение. Сигнатуру `retrieve_with_relaxation(..., area_match: AreaMatch | None = None, ...)`; убрать внутренний вызов `resolve_area` (его теперь делает pipeline, Task 7). Логика:
```python
    # гео-предикаты: точка + область (готовый AreaMatch из pipeline)
    preds, params = [], []
    if point is not None:
        sql, p = point_predicate(point.lon, point.lat, point.minutes, provider, point.mode)
        preds.append(sql); params.extend(p)
    area_widen = list(area_match.widen) if area_match else []
    if area_match:
        preds.append(area_match.sql); params.extend(area_match.params)

    def build_geo(extra_preds, extra_params):
        allp = preds_base + extra_preds
        return (" AND ".join(f"({s})" for s in allp) if allp else None), (params_base + extra_params)
    # preds_base/params_base = предикаты БЕЗ области (точка); область варьируется
```
Реализация: держать `point`-предикат в `base`, `area`-предикат отдельно; в петле, если `len(cands) < min_r` и штатный relax исчерпан — брать следующий шаг из `area_widen`, пересобирать geo_sql, добавлять его label в `relaxed`, пересчитывать. Полный вид:
```python
def retrieve_with_relaxation(conn, pq, *, point=None, provider=None, model=None,
        query_vec=None, min_results=None, max_iters=None,
        area_match=None, search_fn=hybrid_search):
    min_r = min_results if min_results is not None else settings.min_results
    iters = max_iters if max_iters is not None else settings.relaxation_max_iters
    base_sql, base_params = None, []
    if point is not None:
        s, p = point_predicate(point.lon, point.lat, point.minutes, provider, point.mode)
        base_sql, base_params = s, list(p)

    area_sql = area_match.sql if area_match else None
    area_params = list(area_match.params) if area_match else []
    area_steps = list(area_match.widen) if area_match else []

    def geo():
        parts = ([base_sql] if base_sql else []) + ([area_sql] if area_sql else [])
        sql = " AND ".join(f"({x})" for x in parts) if parts else None
        return sql, base_params + area_params

    relaxed, cur_pq = [], pq
    gsql, gpar = geo()
    cands = search_fn(conn, cur_pq, model=model, query_vec=query_vec, geo_sql=gsql, geo_params=gpar)
    for _ in range(iters):
        if len(cands) >= min_r:
            break
        step = relax(cur_pq)
        if step is None:
            break
        cur_pq, note = step
        relaxed.append(note)
        gsql, gpar = geo()
        cands = search_fn(conn, cur_pq, model=model, query_vec=query_vec, geo_sql=gsql, geo_params=gpar)
    # авто-расширение области, если всё ещё мало
    while len(cands) < min_r and area_steps:
        wsql, wpar, wlabel = area_steps.pop(0)
        area_sql = None if wsql == "TRUE" else wsql
        area_params = [] if wsql == "TRUE" else list(wpar)
        relaxed.append(wlabel)
        gsql, gpar = geo()
        cands = search_fn(conn, cur_pq, model=model, query_vec=query_vec, geo_sql=gsql, geo_params=gpar)
    return cands, relaxed, cur_pq
```

- [ ] **Step 5: Запустить — PASS**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_area.py tests/test_orchestrator.py -v`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add habitus/online/geo.py habitus/online/orchestrator.py tests/test_area.py tests/test_orchestrator.py
git commit -m "feat: resolve_area Nominatim-fallback + авто-расширение области в оркестраторе"
```

---

### Task 7: NLU-примеры + разводка в pipeline + интеграция

**Files:**
- Modify: `habitus/online/nlu.py`
- Modify: `habitus/online/pipeline.py`
- Modify: `tests/test_nlu.py`, `tests/test_pipeline.py`

**Interfaces:**
- Consumes: `resolve_area` (Tasks 4–6), `retrieve_with_relaxation(..., area_match=...)` (Task 6).
- Produces: pipeline резолвит `pq.area` в `AreaMatch` и передаёт в оркестратор.

- [ ] **Step 1: NLU-примеры промпта** — в `SYSTEM_PROMPT` (`habitus/online/nlu.py`) в блок про area добавить примеры районов/центра/разговорных:
```
Примеры area: «в Хамовниках»→"Хамовники"; «в центре»→"центр"; «внутри Садового»→"внутри Садового"; «у Патриков»→"Патрики"; «northern Moscow»→"север".
```

- [ ] **Step 2: Тест pipeline — area доезжает до фильтра**

`tests/test_pipeline.py` (рядом с существующими, на dev-фикстуре conn с 2 объектами): в fixture `conn` проставить `okrug` двум объектам ('A'→'ЦАО','B'→'САО'); FakeLLM вернуть `area:"центр"`; проверить, что в выдаче только 'A'. (Код фикстуры и FakeLLM уже есть — расширить INSERT колонкой okrug и `_parse_resp` полем area.)
```python
def test_area_center_filters(conn):
    with conn.cursor() as cur:
        cur.execute("UPDATE listings SET okrug='ЦАО' WHERE external_id='A';")
        cur.execute("UPDATE listings SET okrug='САО' WHERE external_id='B';")
    conn.commit()
    llm = FakeLLM([LLMResponse(content=None, tool_arguments=json.dumps(
        {"rooms": [2], "area": "центр", "semantic_text": "тихо"})), _explain_resp()])
    resp = run_search("двушка в центре", conn, llm=llm, model=FakeModel(), reranker=FakeReranker())
    assert {r.external_id for r in resp.results} == {"A"}
```

- [ ] **Step 3: Запустить — FAIL**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_pipeline.py::test_area_center_filters -v`
Expected: FAIL (pipeline пока не резолвит area).

- [ ] **Step 4: Разводка в `habitus/online/pipeline.py`**

После шага NLU (получив `pq`), перед retrieval, резолвить область и передать в оркестратор:
```python
    from habitus.online.geo import resolve_area
    area_match = None
    if pq.area:
        try:
            with trace.span("resolve_area"):
                area_match = resolve_area(pq.area, conn)
        except Exception as exc:
            log.warning("резолв области не удался: %s", exc, exc_info=True)
    ...
    cands, relaxed, _ = retrieve_with_relaxation(
        conn, search_pq, point=point, provider=provider,
        model=model, query_vec=query_vec, area_match=area_match)
```
(Убрать передачу `geocoder`/`pq.area` внутрь оркестратора — теперь он принимает готовый `area_match`.)

- [ ] **Step 5: Запустить — PASS**

Run: `DB_DSN="…/habitus_test" uv run pytest tests/test_pipeline.py::test_area_center_filters tests/test_nlu.py -v`
Expected: PASS.

- [ ] **Step 6: Полный сьют на scratch-БД**

Run: `PGPASSWORD=habitus psql -h localhost -p 5544 -U habitus -d habitus -c "DROP DATABASE IF EXISTS habitus_test;" -c "CREATE DATABASE habitus_test;"` затем `DB_DSN="…/habitus_test" uv run pytest -q`
Expected: всё зелёное.

- [ ] **Step 7: Commit**

```bash
git add habitus/online/nlu.py habitus/online/pipeline.py tests/test_nlu.py tests/test_pipeline.py
git commit -m "feat: разводка area в pipeline — резолв зоны и передача в оркестратор"
```

---

### Task 8: Наполнение данных + живая проверка (dev-БД)

**Files:** нет кода — операционная задача на dev-БД (порт 5544, с циановскими данными).

- [ ] **Step 1: Собрать GeoJSON зон**

Run: `uv run python scripts/build_zones_geojson.py --out data/moscow_admin.geojson`
Проверить: ~12 округов + ~120 районов, минимум ПРОПУСК'ов. При многих пропусках — ручная выгрузка osm-boundaries.com (admin_level 5+8) в тот же файл.

- [ ] **Step 2: Импорт в dev-БД**

Run: `uv run habitus import-zones --geojson data/moscow_admin.geojson`
Expected: `{'admin_zones': ~135, 'named_zones': ~7, 'listings_backfilled': 979}`.
Проверить распределение: `SELECT okrug, count(*) FROM listings GROUP BY okrug ORDER BY 2 DESC;` — ЦАО должен быть крупнейшим.

- [ ] **Step 3: Живой smoke резолвера**

Run (перезапустить нативный ml или через `uv run habitus search`): «двушка на севере москвы» → проверить, что все объекты в окр. САО/СВАО/СЗАО; «в центре» → все ЦАО; «в Хамовниках» → raion=Хамовники; «у Патриков» → около Патриарших.

- [ ] **Step 4: Обновить README**

Добавить в раздел «Наполнение данными» команду `import-zones` (аналогично `import-evidence`). Commit.

```bash
git add README.md
git commit -m "docs: import-zones в README"
```
